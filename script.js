const intro=document.querySelector('#intro'),app=document.querySelector('#app'),enter=document.querySelector('#enter'),card=document.querySelector('.tilt'),copy=document.querySelector('#copyDiscord'),play=document.querySelector('#play'),audio=document.querySelector('#audio'),music=document.querySelector('.music');
enter.addEventListener('click',()=>{intro.classList.add('hide');app.classList.add('show');app.setAttribute('aria-hidden','false')});
copy.addEventListener('click',async()=>{try{await navigator.clipboard.writeText('jozz7x');copy.querySelector('small').textContent='copied ✓';setTimeout(()=>copy.querySelector('small').textContent='@jozz7x · copy',1500)}catch{}});
play.addEventListener('click',()=>{if(!audio.src){play.textContent='·';music.querySelector('small').textContent='audio file not installed';return}audio.paused?audio.play():audio.pause()});audio.addEventListener('play',()=>{music.classList.add('playing');play.textContent='Ⅱ'});audio.addEventListener('pause',()=>{music.classList.remove('playing');play.textContent='▶'});
if(matchMedia('(pointer:fine)').matches){card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1100px) rotateX(${-y*1.8}deg) rotateY(${x*1.8}deg)`});card.addEventListener('mouseleave',()=>card.style.transform='')}
const c=document.querySelector('#particles'),ctx=c.getContext('2d');let dots=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);dots=Array.from({length:Math.min(65,Math.floor(innerWidth/18))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*.8+.15,v:Math.random()*.12+.025,a:Math.random()*.25+.05}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const d of dots){d.y-=d.v;if(d.y<0){d.y=innerHeight;d.x=Math.random()*innerWidth}ctx.beginPath();ctx.arc(d.x,d.y,d.r,0,Math.PI*2);ctx.fillStyle=`rgba(255,255,255,${d.a})`;ctx.fill()}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();if(!matchMedia('(prefers-reduced-motion:reduce)').matches)draw();
// Privacy-friendly local visit threshold. Replace with a serverless counter later for global unique visits.
const n=Number(localStorage.getItem('scorpi-visits')||0)+1;localStorage.setItem('scorpi-visits',n);if(n>=100){document.querySelector('#views').hidden=false;document.querySelector('#views span').textContent=n}